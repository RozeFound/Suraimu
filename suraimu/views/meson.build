pkgdatadir = join_paths(get_option('prefix'), get_option('datadir'), meson.project_name())
viewsdir = join_paths(pkgdatadir, 'suraimu/views')

suraimu_sources = [
  '__init__.py',
  'not_implemented.py',
  'library.py'
]

install_data(suraimu_sources, install_dir: viewsdir)